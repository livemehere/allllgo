#include <vector>
#include <iostream>
#include <queue>

using namespace std;

class Solution
{
public:
    vector<vector<int>> res;
    vector<int> path;
    vector<vector<int>> permute(vector<int> &nums)
    {
        for (int i = 0; i < nums.size(); i++)
        {
            get(i)
        }
        return res;
    }

    vector<vector<int>> get(vector<int> arr)
    {
        vector<int> slice = copy(arr.begin() + 1, );

        return copy;
    }
};

int main()
{

    Solution s;

    return 0;
}